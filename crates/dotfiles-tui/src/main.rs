//! `dotfiles-tui` — the human surface (ADR-001 #4): a live projection of the
//! always-fresh dotfiles state (ADR-005), inventory-first.
//!
//! v0.1 skeleton. The ratatui inventory view and the always-fresh watch loop
//! are wired in the next slice; for now use `dotfiles-cli status`.

fn main() {
    eprintln!("dotfiles-tui: inventory TUI not yet wired (v0.1 skeleton).");
    eprintln!("Use `dotfiles-cli status --manifest <file>` for the JSON catalog.");
}
